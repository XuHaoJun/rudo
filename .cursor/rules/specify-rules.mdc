# rudo Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-03

## Active Technologies
- Rust 1.75+ (stable) + `parking_lot` (existing), `crossbeam-queue` (new for lock-free worklist) (008-incremental-marking)
- N/A (in-memory GC) (008-incremental-marking)
- Rust 1.75+ (stable) + None (std-only: `std::sync::atomic`, `std::cell::UnsafeCell`, `std::time`) (010-gc-metrics-v2)
- In-memory only (thread-local `Cell`, static atomics, static ring buffer) (010-gc-metrics-v2)

- Rust 1.75+ (stable) + parking_lot (mutex), std::sync::atomic (007-gen-gc-dirty-pages)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] cargo clippy

## Code Style

Rust 1.75+ (stable): Follow standard conventions

## Recent Changes
- 010-gc-metrics-v2: Added Rust 1.75+ (stable) + None (std-only: `std::sync::atomic`, `std::cell::UnsafeCell`, `std::time`)
- 008-incremental-marking: Added Rust 1.75+ (stable) + `parking_lot` (existing), `crossbeam-queue` (new for lock-free worklist)

- 007-gen-gc-dirty-pages: Added Rust 1.75+ (stable) + parking_lot (mutex), std::sync::atomic

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
